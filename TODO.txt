camera distance control with scroll wheel?

tools-
	-draw 2D plane to define the negative space to slice
	-radial carving tool, place center by clicking or text entry

change shader to use STATE array

functions to set STATE array to 0=removed, 1=external, 2=internal for each point by looking at neighboors

converting external points into a mesh, then smoothing it

saving/loading (pickles)

exporter